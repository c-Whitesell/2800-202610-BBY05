  <%- include('components/header', { 
  title: 'Map',
  styles: `
    <link href="https://unpkg.com/maplibre-gl@5.23.0/dist/maplibre-gl.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/map.css" />
  `
  }) %>

    <div id="map"></div>
  <%- include('components/date-time-picker') %>

    <!-- Include the instruction partial here -->
    <%- include('components/map-tutorial') %>

    <script>
  // Use EJS tags to inject the values directly into client-side JS
  window.MAP_CONFIG = {
    initLat: <%= typeof initLat !== 'undefined' && initLat ? initLat : 49.2827 %>,
    initLng: <%= typeof initLng !== 'undefined' && initLng ? initLng : -123.1207 %>,
    initZoom: <%= typeof initZoom !== 'undefined' && initZoom ? initZoom : 11 %>
  };

</script>

<%- include('components/footer') %>
